import React from 'react';

const Register = () => {
    return (
        // <div>
        // <form onSubmit={handleRegister}>
        // <input onChange={handleEmailChange} type="email" name='email' id='' placeholder='Your Email'></input> <br />
        // {/* to verify the strength of the password */}
        {/* <input onChange={handlePasswordChange} type="password" name='password' id='' placeholder='Your Password'></input> <br />
        <button type='submit'>Register</button>
      </form>
        </div> */}
    );
};

export default Register;